# Retail Support Agent 开发规则

这是此学习分支自定义的代码助手指南，不是 GitHub 平台规则。用户任务决定工作范围，平台权限和安全要求仍适用。本文件替换原上游强制技能、分阶段审查、发布及交付流程。

## 范围与实现

- 核心应用是 `examples/support_app/`。优先在应用层实现功能，仅在任务需要时修改 `src/agents/`。
- 先阅读现有代码和差异，保留用户已有工作，不覆盖无关文件。
- 订单事实来自工具。历史发票不是实时订单；物流、付款、退款等缺失信息必须明确未知。
- 客户范围由宿主上下文决定，工具不能让模型切换客户。CLI 选择客户不等于登录。
- 工单必须先展示内容并得到明确确认，保持串行确认和重复请求处理。不宣称本地工单已经通知商家。
- 明确区分公开历史数据、虚构政策和真实业务承诺。会话按客户隔离，数据迁移不得覆盖用户记录。
- 网络等待使用异步接口或合适的线程转移，保留异常处理和资源释放。
- 不打印或提交密钥，不提交数据库、聊天、下载文件和本地日志。

## 验证与复核

文档修改核对代码事实、命令和链接，并运行 `git diff --check`。客服逻辑修改运行应用单元测试、Ruff 检查与格式检查；为实际行为变化添加有效回归测试。SDK 或共享配置修改追加受影响测试，较大变更执行 `make format-check`、`make lint`、`make typecheck`、`make tests`。

重点复核客户隔离、确认后写入、金额精度、事务回滚、会话隔离和异常路径。独立审查不作为每次任务的固定关卡，仅按用户要求或任务需要且工具允许时使用。不得删除断言或绕过检查来掩盖失败。

报告实际通过、未运行及失败原因。离线测试不代表在线 API 验证，局部通过不代表全量通过。具体命令见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 提交与辅助资料

按用户授权范围提交和推送，检查待提交内容，不强推或覆盖无关工作。交付说明修改、验证与剩余限制。

`.agents/` 中保留的上游辅助资料与脚本用于兼容现有工具链，不自动启用，也不构成本分支强制流程。需要参考时以当前任务和本文件为准。旧审查入口已改写为可选的客服复核清单。
